--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -2494,7 +2494,9 @@
         // evict checkpoints within min-step of a previous checkpoint, unless they were
         // created by the current task
         int64_t last = -1;
-        for (auto it = slot.prompt.checkpoints.begin(); it != slot.prompt.checkpoints.end(); ) {
+        for (auto it = slot.prompt.checkpoints.begin();
+                slot.prompt.checkpoints.size() + 1 >= (size_t) params_base.n_ctx_checkpoints &&
+                it != slot.prompt.checkpoints.end(); ) {
             if (it->id_task != id_task && last >= 0 && it->n_tokens <= last + params_base.checkpoint_min_step) {
                 SLT_TRC(slot, "erasing context checkpoint too close to an earlier one (pos_min = %d, pos_max = %d, n_tokens = %" PRId64 ", size = %.3f MiB)\n",
                         it->pos_min, it->pos_max, it->n_tokens, (float) it->size() / 1024 / 1024);
@@ -2518,6 +2520,18 @@
 
             slot.prompt.checkpoint_buffer_pool.splice(
                     slot.prompt.checkpoint_buffer_pool.end(), slot.prompt.checkpoints, slot.prompt.checkpoints.begin());
+        }
+
+        // Preserve our reusable checkpoint buffers while replacing duplicate positions.
+        const int64_t n_tokens_new = slot.prompt.n_tokens() - n_tokens_cur;
+        for (auto it = slot.prompt.checkpoints.begin(); it != slot.prompt.checkpoints.end(); ) {
+            if (it->n_tokens == n_tokens_new) {
+                auto duplicate = it++;
+                slot.prompt.checkpoint_buffer_pool.splice(
+                        slot.prompt.checkpoint_buffer_pool.end(), slot.prompt.checkpoints, duplicate);
+            } else {
+                ++it;
+            }
         }
 
         if (slot.prompt.checkpoint_buffer_pool.empty()) {
