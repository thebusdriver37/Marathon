diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
index 772a8a0a96..75a9b5aae7 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
@@ -14245,6 +14245,50 @@
         ],
         "type": "object"
       },
+      "MarathonResponseMetrics": {
+        "description": "Backend decode and prefill totals for a completed local response.",
+        "properties": {
+          "backendCalls": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "decodeMicroseconds": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "decodeTokens": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "prefillMicroseconds": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "prefillTokens": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          },
+          "timedBackendCalls": {
+            "format": "uint64",
+            "minimum": 0.0,
+            "type": "integer"
+          }
+        },
+        "required": [
+          "backendCalls",
+          "decodeMicroseconds",
+          "decodeTokens",
+          "prefillMicroseconds",
+          "prefillTokens",
+          "timedBackendCalls"
+        ],
+        "type": "object"
+      },
       "MarketplaceAddParams": {
         "$schema": "http://json-schema.org/draft-07/schema#",
         "properties": {
@@ -19148,6 +19192,16 @@
               "null"
             ]
           },
+          "marathon": {
+            "anyOf": [
+              {
+                "$ref": "#/definitions/v2/MarathonResponseMetrics"
+              },
+              {
+                "type": "null"
+              }
+            ]
+          },
           "metadata": true
         },
         "type": "object"
diff --git a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
index ec8196f085..1738e2ba50 100644
--- a/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
+++ b/codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
@@ -10297,6 +10297,50 @@
       ],
       "type": "object"
     },
+    "MarathonResponseMetrics": {
+      "description": "Backend decode and prefill totals for a completed local response.",
+      "properties": {
+        "backendCalls": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "decodeMicroseconds": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "decodeTokens": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "prefillMicroseconds": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "prefillTokens": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "timedBackendCalls": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        }
+      },
+      "required": [
+        "backendCalls",
+        "decodeMicroseconds",
+        "decodeTokens",
+        "prefillMicroseconds",
+        "prefillTokens",
+        "timedBackendCalls"
+      ],
+      "type": "object"
+    },
     "MarketplaceAddParams": {
       "$schema": "http://json-schema.org/draft-07/schema#",
       "properties": {
@@ -15200,6 +15244,16 @@
             "null"
           ]
         },
+        "marathon": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MarathonResponseMetrics"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "metadata": true
       },
       "type": "object"
diff --git a/codex-rs/app-server-protocol/schema/json/v2/RawResponseCompletedNotification.json b/codex-rs/app-server-protocol/schema/json/v2/RawResponseCompletedNotification.json
index 5b65b9dfc2..d4a4e3945f 100644
--- a/codex-rs/app-server-protocol/schema/json/v2/RawResponseCompletedNotification.json
+++ b/codex-rs/app-server-protocol/schema/json/v2/RawResponseCompletedNotification.json
@@ -1,6 +1,50 @@
 {
   "$schema": "http://json-schema.org/draft-07/schema#",
   "definitions": {
+    "MarathonResponseMetrics": {
+      "description": "Backend decode and prefill totals for a completed local response.",
+      "properties": {
+        "backendCalls": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "decodeMicroseconds": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "decodeTokens": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "prefillMicroseconds": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "prefillTokens": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        },
+        "timedBackendCalls": {
+          "format": "uint64",
+          "minimum": 0.0,
+          "type": "integer"
+        }
+      },
+      "required": [
+        "backendCalls",
+        "decodeMicroseconds",
+        "decodeTokens",
+        "prefillMicroseconds",
+        "prefillTokens",
+        "timedBackendCalls"
+      ],
+      "type": "object"
+    },
     "ResponseUsageMetadata": {
       "description": "Usage metadata reported for one upstream response.",
       "properties": {
@@ -10,6 +54,16 @@
             "null"
           ]
         },
+        "marathon": {
+          "anyOf": [
+            {
+              "$ref": "#/definitions/MarathonResponseMetrics"
+            },
+            {
+              "type": "null"
+            }
+          ]
+        },
         "metadata": true
       },
       "type": "object"
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/MarathonResponseMetrics.ts b/codex-rs/app-server-protocol/schema/typescript/v2/MarathonResponseMetrics.ts
new file mode 100644
index 0000000000..f2c066adbc
--- /dev/null
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/MarathonResponseMetrics.ts
@@ -0,0 +1,8 @@
+// GENERATED CODE! DO NOT MODIFY BY HAND!
+
+// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
+
+/**
+ * Backend decode and prefill totals for a completed local response.
+ */
+export type MarathonResponseMetrics = { decodeTokens: number, decodeMicroseconds: number, prefillTokens: number, prefillMicroseconds: number, backendCalls: number, timedBackendCalls: number, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/ResponseUsageMetadata.ts b/codex-rs/app-server-protocol/schema/typescript/v2/ResponseUsageMetadata.ts
index 34b492bed2..0f6a8d20e3 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/ResponseUsageMetadata.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/ResponseUsageMetadata.ts
@@ -2,8 +2,9 @@
 
 // This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
 import type { JsonValue } from "../serde_json/JsonValue";
+import type { MarathonResponseMetrics } from "./MarathonResponseMetrics";
 
 /**
  * Usage metadata reported for one upstream response.
  */
-export type ResponseUsageMetadata = { amount: string | null, metadata: JsonValue | null, };
+export type ResponseUsageMetadata = { amount: string | null, metadata: JsonValue | null, marathon: MarathonResponseMetrics | null, };
diff --git a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
index 64c7420cc0..a233e29b56 100644
--- a/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
+++ b/codex-rs/app-server-protocol/schema/typescript/v2/index.ts
@@ -242,6 +242,7 @@ export type { LoginAccountResponse } from "./LoginAccountResponse";
 export type { LoginAppBrand } from "./LoginAppBrand";
 export type { LogoutAccountResponse } from "./LogoutAccountResponse";
 export type { ManagedHooksRequirements } from "./ManagedHooksRequirements";
+export type { MarathonResponseMetrics } from "./MarathonResponseMetrics";
 export type { MarketplaceAddParams } from "./MarketplaceAddParams";
 export type { MarketplaceAddResponse } from "./MarketplaceAddResponse";
 export type { MarketplaceInterface } from "./MarketplaceInterface";
diff --git a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
index 09732cfb43..b3b13ce0e1 100644
--- a/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
+++ b/codex-rs/app-server-protocol/src/protocol/v2/thread.rs
@@ -1872,6 +1872,26 @@ pub struct RawResponseCompletedNotification {
 pub struct ResponseUsageMetadata {
     pub amount: Option<String>,
     pub metadata: Option<JsonValue>,
+    pub marathon: Option<MarathonResponseMetrics>,
+}
+
+/// Backend decode and prefill totals for a completed local response.
+#[derive(Serialize, Deserialize, Debug, Clone, Default, PartialEq, JsonSchema, TS)]
+#[serde(rename_all = "camelCase")]
+#[ts(export_to = "v2/")]
+pub struct MarathonResponseMetrics {
+    #[ts(type = "number")]
+    pub decode_tokens: u64,
+    #[ts(type = "number")]
+    pub decode_microseconds: u64,
+    #[ts(type = "number")]
+    pub prefill_tokens: u64,
+    #[ts(type = "number")]
+    pub prefill_microseconds: u64,
+    #[ts(type = "number")]
+    pub backend_calls: u64,
+    #[ts(type = "number")]
+    pub timed_backend_calls: u64,
 }
 
 impl From<codex_protocol::ResponseUsageMetadata> for ResponseUsageMetadata {
@@ -1879,6 +1899,14 @@ impl From<codex_protocol::ResponseUsageMetadata> for ResponseUsageMetadata {
         Self {
             amount: value.amount,
             metadata: value.metadata,
+            marathon: value.marathon.map(|metrics| MarathonResponseMetrics {
+                decode_tokens: metrics.decode_tokens,
+                decode_microseconds: metrics.decode_microseconds,
+                prefill_tokens: metrics.prefill_tokens,
+                prefill_microseconds: metrics.prefill_microseconds,
+                backend_calls: metrics.backend_calls,
+                timed_backend_calls: metrics.timed_backend_calls,
+            }),
         }
     }
 }
diff --git a/codex-rs/app-server/src/request_processors/thread_lifecycle.rs b/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
index b0fa49b4ba..8bbd7e09cd 100644
--- a/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
+++ b/codex-rs/app-server/src/request_processors/thread_lifecycle.rs
@@ -325,10 +325,15 @@ pub(super) async fn ensure_listener_task_running(
                         thread_state.track_current_turn_event(&event.id, &event.msg);
                         thread_state.experimental_raw_events
                     };
+                    let has_local_metrics = matches!(
+                        &event.msg,
+                        EventMsg::RawResponseCompleted(completed)
+                            if completed.usage_metadata.as_ref().is_some_and(|metadata| metadata.marathon.is_some())
+                    );
                     if matches!(
                         &event.msg,
                         EventMsg::RawResponseItem(_) | EventMsg::RawResponseCompleted(_)
-                    ) && !raw_events_enabled
+                    ) && !raw_events_enabled && !has_local_metrics
                     {
                         continue;
                     }
diff --git a/codex-rs/app-server/tests/suite/v2/compaction.rs b/codex-rs/app-server/tests/suite/v2/compaction.rs
index 9f3d1a0e61..5b7cd33660 100644
--- a/codex-rs/app-server/tests/suite/v2/compaction.rs
+++ b/codex-rs/app-server/tests/suite/v2/compaction.rs
@@ -257,6 +257,7 @@ async fn thread_compact_start_triggers_compaction_and_returns_empty_response() -
             usage_metadata: Some(ResponseUsageMetadata {
                 amount: Some("0.125".to_string()),
                 metadata: Some(expected_metadata),
+                marathon: None,
             }),
             usage: Some(TokenUsageBreakdown {
                 total_tokens: 200,
diff --git a/codex-rs/app-server/tests/suite/v2/turn_start.rs b/codex-rs/app-server/tests/suite/v2/turn_start.rs
index 4179365c1e..896d27a302 100644
--- a/codex-rs/app-server/tests/suite/v2/turn_start.rs
+++ b/codex-rs/app-server/tests/suite/v2/turn_start.rs
@@ -1163,25 +1163,54 @@ async fn turn_start_sends_service_tier_id_to_model_request() -> Result<()> {
     Ok(())
 }
 
-#[test_case(None, None, None; "without_usage_metadata")]
-#[test_case(Some(json!({})), None, None; "without_amount")]
-#[test_case(Some(json!({ "amount": null })), None, None; "null_amount")]
-#[test_case(Some(json!({ "amount": "0" })), Some("0"), None; "zero_amount")]
+#[test_case(None, None, None, None; "without_usage_metadata")]
+#[test_case(Some(json!({})), None, None, None; "without_amount")]
+#[test_case(Some(json!({ "amount": null })), None, None, None; "null_amount")]
+#[test_case(Some(json!({ "amount": "0" })), Some("0"), None, None; "zero_amount")]
+#[test_case(
+    Some(json!({"marathon": {
+        "context_usage": {"input_tokens": 20, "cached_input_tokens": 7,
+            "cache_write_input_tokens": 0, "output_tokens": 5,
+            "reasoning_output_tokens": 3, "total_tokens": 25},
+        "decode_tokens": 5, "decode_microseconds": 100000,
+        "prefill_tokens": 13, "prefill_microseconds": 200000,
+        "backend_calls": 2, "timed_backend_calls": 2
+    }})),
+    None,
+    Some(json!({"label": "local response"})),
+    Some(json!({
+        "decodeTokens": 5, "decodeMicroseconds": 100000,
+        "prefillTokens": 13, "prefillMicroseconds": 200000,
+        "backendCalls": 2, "timedBackendCalls": 2
+    }));
+    "marathon_decode_and_context"
+)]
 #[test_case(
     Some(json!({ "amount": "0.12345678901234567890" })),
     Some("0.12345678901234567890"),
-    Some(json!({ "label": "example", "items": [0, null, true] }));
+    Some(json!({ "label": "example", "items": [0, null, true] })),
+    None;
     "exact_amount"
 )]
-#[test_case(None, None, Some(json!(null)); "null_extra")]
-#[test_case(None, None, Some(json!(0)); "zero_extra")]
-#[test_case(None, None, Some(json!({ "label": "example" })); "nested_extra")]
+#[test_case(None, None, Some(json!(null)), None; "null_extra")]
+#[test_case(None, None, Some(json!(0)), None; "zero_extra")]
+#[test_case(None, None, Some(json!({ "label": "example" })), None; "nested_extra")]
 #[tokio::test]
 async fn turn_start_emits_raw_response_completed_with_upstream_usage(
     upstream_metadata: Option<Value>,
     expected_amount: Option<&str>,
     extra: Option<Value>,
+    expected_marathon: Option<Value>,
 ) -> Result<()> {
+    let context_total = upstream_metadata
+        .as_ref()
+        .and_then(|metadata| metadata.pointer("/marathon/context_usage/total_tokens"))
+        .and_then(Value::as_i64)
+        .unwrap_or(37);
+    let raw_events_enabled = upstream_metadata
+        .as_ref()
+        .and_then(|metadata| metadata.get("marathon"))
+        .is_none();
     let server = responses::start_mock_server().await;
     let mut completed = json!({
         "type": "response.completed",
@@ -1205,6 +1234,7 @@ async fn turn_start_emits_raw_response_completed_with_upstream_usage(
     let expected_metadata = json!({
         "amount": expected_amount,
         "metadata": completed["response"]["usage"],
+        "marathon": expected_marathon,
     });
     let body = responses::sse(vec![
         responses::ev_response_created("resp-1"),
@@ -1224,7 +1254,7 @@ async fn turn_start_emits_raw_response_completed_with_upstream_usage(
 
     let ThreadStartResponse { thread, .. } = mcp
         .start_thread(ThreadStartParams {
-            experimental_raw_events: true,
+            experimental_raw_events: raw_events_enabled,
             ..Default::default()
         })
         .await?;
@@ -1265,7 +1295,7 @@ async fn turn_start_emits_raw_response_completed_with_upstream_usage(
     assert_eq!(
         notification,
         RawResponseCompletedNotification {
-            thread_id: thread.id,
+            thread_id: thread.id.clone(),
             turn_id: turn.id,
             response_id: "resp-1".to_string(),
             usage_metadata: serde_json::from_value(expected_metadata)?,
@@ -1281,6 +1311,17 @@ async fn turn_start_emits_raw_response_completed_with_upstream_usage(
     );
 
     response_mock.single_request();
+    let usage_notification = timeout(
+        DEFAULT_READ_TIMEOUT,
+        mcp.read_stream_until_notification_message("thread/tokenUsage/updated"),
+    )
+    .await??;
+    let usage = usage_notification.params.expect("token usage notification");
+    assert_eq!(usage["tokenUsage"]["total"]["totalTokens"], json!(37));
+    assert_eq!(
+        usage["tokenUsage"]["last"]["totalTokens"],
+        json!(context_total)
+    );
     Ok(())
 }
 
diff --git a/codex-rs/codex-api/tests/sse_end_to_end.rs b/codex-rs/codex-api/tests/sse_end_to_end.rs
index 5c1dcb8ee5..842b5e163c 100644
--- a/codex-rs/codex-api/tests/sse_end_to_end.rs
+++ b/codex-rs/codex-api/tests/sse_end_to_end.rs
@@ -177,6 +177,7 @@ async fn responses_stream_parses_items_and_completed_end_to_end() -> Result<()>
                 &Some(codex_protocol::ResponseUsageMetadata {
                     amount: Some("0.12345678901234567890".to_string()),
                     metadata: Some(expected_metadata),
+                    marathon: None,
                 })
             );
             assert_eq!(
diff --git a/codex-rs/core/src/compact_remote_v2.rs b/codex-rs/core/src/compact_remote_v2.rs
index a6436d15f7..52b8d77e8d 100644
--- a/codex-rs/core/src/compact_remote_v2.rs
+++ b/codex-rs/core/src/compact_remote_v2.rs
@@ -1227,6 +1227,7 @@ mod tests {
                 usage_metadata: Some(codex_protocol::ResponseUsageMetadata {
                     amount: Some("0.125".to_string()),
                     metadata: Some(serde_json::json!({ "extra": { "label": "example" } })),
+                    marathon: None,
                 }),
                 end_turn: Some(true),
             }),
@@ -1250,7 +1251,8 @@ mod tests {
             Some(codex_protocol::ResponseUsageMetadata {
                 amount: Some("0.125".to_string()),
                 metadata: Some(serde_json::json!({ "extra": { "label": "example" } })),
-            })
+                marathon: None,
+            }),
         );
         assert_eq!(
             output.token_usage,
diff --git a/codex-rs/core/src/session/mod.rs b/codex-rs/core/src/session/mod.rs
index a4b4ba0032..774e6bd374 100644
--- a/codex-rs/core/src/session/mod.rs
+++ b/codex-rs/core/src/session/mod.rs
@@ -4618,17 +4618,38 @@ impl Session {
         turn_context: &TurnContext,
         settings: &ResolvedStepSettings,
         token_usage: Option<&TokenUsage>,
+    ) -> CodexResult<()> {
+        self.record_response_usage_info(turn_context, settings, token_usage, /*context_usage*/ None)
+            .await
+    }
+
+    pub(crate) async fn record_response_usage_info(
+        &self,
+        turn_context: &TurnContext,
+        settings: &ResolvedStepSettings,
+        token_usage: Option<&TokenUsage>,
+        context_usage: Option<&TokenUsage>,
     ) -> CodexResult<()> {
         if let Some(token_usage) = token_usage {
             let token_info = {
                 let mut state = self.state.lock().await;
                 state
                     .update_token_info_from_usage(token_usage, turn_context.model_context_window());
+                // A router-managed tool loop has cumulative usage but only its
+                // final model call describes the currently occupied context.
+                if let Some(context_usage) = context_usage
+                    && let Some(mut info) = state.token_info()
+                {
+                    info.last_token_usage = context_usage.clone();
+                    state.set_token_info(Some(info));
+                }
                 if matches!(
                     turn_context.config.model_auto_compact_token_limit_scope,
                     AutoCompactTokenLimitScope::BodyAfterPrefix
                 ) {
-                    state.ensure_auto_compact_window_server_prefill_from_usage(token_usage);
+                    state.ensure_auto_compact_window_server_prefill_from_usage(
+                        context_usage.unwrap_or(token_usage),
+                    );
                 }
                 state.token_info()
             };
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index 6cfa5cf0ab..639a5b251e 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -2830,6 +2830,10 @@ async fn try_run_sampling_request(
                 usage_metadata,
                 end_turn,
             } => {
+                let context_usage = usage_metadata
+                    .as_ref()
+                    .and_then(|metadata| metadata.marathon.as_ref())
+                    .and_then(|metrics| metrics.context_usage.clone());
                 sess.services
                     .analytics_events_client
                     .track_code_mode_tool_call(
@@ -2855,10 +2859,11 @@ async fn try_run_sampling_request(
                 )
                 .await;
                 let budget_result = sess
-                    .record_token_usage_info(
+                    .record_response_usage_info(
                         &turn_context,
                         &step_context.settings,
                         token_usage.as_ref(),
+                        context_usage.as_ref(),
                     )
                     .await;
                 should_emit_token_count = true;
diff --git a/codex-rs/core/tests/suite/client_websockets.rs b/codex-rs/core/tests/suite/client_websockets.rs
index b9a0492e1a..851e149515 100755
--- a/codex-rs/core/tests/suite/client_websockets.rs
+++ b/codex-rs/core/tests/suite/client_websockets.rs
@@ -203,6 +203,7 @@ async fn responses_websocket_preserves_credit_usage_metadata() {
         Some(codex_protocol::ResponseUsageMetadata {
             amount: Some("0.12345678901234567890".to_string()),
             metadata: Some(expected_metadata),
+            marathon: None,
         }),
     );
     assert_eq!(server.single_connection().len(), 1);
diff --git a/codex-rs/protocol/src/lib.rs b/codex-rs/protocol/src/lib.rs
index c98a2f57b3..bf57e5e599 100644
--- a/codex-rs/protocol/src/lib.rs
+++ b/codex-rs/protocol/src/lib.rs
@@ -11,6 +11,7 @@ pub use agent_path::AgentPath;
 pub use permission_profile_intersection::PermissionIntersectionError;
 pub use permission_profile_intersection::intersect_effective_permission_profiles;
 pub use response_item_id::ResponseItemId;
+pub use response_usage::MarathonResponseMetrics;
 pub use response_usage::ResponseUsageMetadata;
 pub use sanitized_git_url::SanitizedGitUrl;
 pub use session_id::SessionId;
diff --git a/codex-rs/protocol/src/response_usage.rs b/codex-rs/protocol/src/response_usage.rs
index 83383bf829..1f61168355 100644
--- a/codex-rs/protocol/src/response_usage.rs
+++ b/codex-rs/protocol/src/response_usage.rs
@@ -11,4 +11,18 @@ use ts_rs::TS;
 pub struct ResponseUsageMetadata {
     pub amount: Option<String>,
     pub metadata: Option<Value>,
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    pub marathon: Option<MarathonResponseMetrics>,
+}
+
+/// Local backend measurements, kept separate from cumulative response usage.
+#[derive(Debug, Default, Clone, Deserialize, Serialize, PartialEq, Eq, TS, JsonSchema)]
+pub struct MarathonResponseMetrics {
+    pub context_usage: Option<crate::protocol::TokenUsage>,
+    pub decode_tokens: u64,
+    pub decode_microseconds: u64,
+    pub prefill_tokens: u64,
+    pub prefill_microseconds: u64,
+    pub backend_calls: u64,
+    pub timed_backend_calls: u64,
 }
