diff --git a/codex-rs/Cargo.lock b/codex-rs/Cargo.lock
index 28071ca8b940fd6783f7205f43fc695576efc70a..76ff159fc6c3d4f4624e9e06be3e549e1f62e070 100644
--- a/codex-rs/Cargo.lock
+++ b/codex-rs/Cargo.lock
@@ -4522,6 +4522,7 @@ dependencies = [
  "codex-shell-command",
  "pretty_assertions",
  "serde",
+ "serial_test",
  "toml 0.9.11+spec-1.1.0",
 ]

diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index 610891ea3dd3b4a1affcaeb7a6567274b76c8de4..46834d3a9f7fdad8a0fc335c8e093b216259017a 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -1212,13 +1212,14 @@ async fn run_ratatui_app(
             terminal_restore_guard.restore_silently();
             session_log::log_session_end();
             let _ = tui.terminal.clear();
+            let action_command = codex_utils_cli::cli_command_for_action(action);
             Ok(AppExitInfo {
                 token_usage: crate::token_usage::TokenUsage::default(),
                 thread_id: None,
                 resume_hint: None,
                 update_action: None,
                 exit_reason: ExitReason::Fatal(format!(
-                    "No saved session found with ID {id_str}. Run `codex {action}` without an ID to choose from existing sessions."
+                    "No saved session found with ID {id_str}. Run `{action_command}` without an ID to choose from existing sessions."
                 )),
             })
         };
diff --git a/codex-rs/utils/cli/Cargo.toml b/codex-rs/utils/cli/Cargo.toml
index 9d330c5ce6badd0d15e6c401cd777fb3d13c8d08..32385f01d27629602e6316bceb5f6cbcec4729dc 100644
--- a/codex-rs/utils/cli/Cargo.toml
+++ b/codex-rs/utils/cli/Cargo.toml
@@ -16,6 +16,7 @@ toml = { workspace = true }

 [dev-dependencies]
 pretty_assertions = { workspace = true }
+serial_test = { workspace = true }

 [lib]
 doctest = false
diff --git a/codex-rs/utils/cli/src/lib.rs b/codex-rs/utils/cli/src/lib.rs
index 633638e72d65a931f68a503d523c76f75d0ac62b..ac0819c71d1cd5b29b230ad1df73ae0bc2fb5e6e 100644
--- a/codex-rs/utils/cli/src/lib.rs
+++ b/codex-rs/utils/cli/src/lib.rs
@@ -9,6 +9,7 @@ pub use approval_mode_cli_arg::ApprovalModeCliArg;
 pub use codex_protocol::config_types::ProfileV2Name;
 pub use config_override::CliConfigOverrides;
 pub use format_env_display::format_env_display;
+pub use resume_command::cli_command_for_action;
 pub use resume_command::resume_command;
 pub use resume_command::resume_hint;
 pub use sandbox_mode_cli_arg::SandboxModeCliArg;
diff --git a/codex-rs/utils/cli/src/resume_command.rs b/codex-rs/utils/cli/src/resume_command.rs
index d3c683905739e159e55e45d78318ed0f15ec81ec..868a67ed0b126d0444d38cbff77ca831ca8bf479 100644
--- a/codex-rs/utils/cli/src/resume_command.rs
+++ b/codex-rs/utils/cli/src/resume_command.rs
@@ -2,6 +2,9 @@

 use codex_protocol::ThreadId;
 use codex_shell_command::parse_command::shlex_join;
+use std::env;
+
+pub const CODEX_CLI_NAME_ENV_VAR: &str = "CODEX_CLI_NAME";

 pub fn resume_command(thread_name: Option<&str>, thread_id: Option<ThreadId>) -> Option<String> {
     let resume_target = thread_name
@@ -9,16 +12,33 @@ pub fn resume_command(thread_name: Option<&str>, thread_id: Option<ThreadId>) ->
         .map(str::to_string)
         .or_else(|| thread_id.map(|thread_id| thread_id.to_string()));
     resume_target.map(|target| {
+        let command = cli_command_for_action("resume");
         let needs_double_dash = target.starts_with('-');
         let escaped = shlex_join(&[target]);
         if needs_double_dash {
-            format!("codex resume -- {escaped}")
+            format!("{command} -- {escaped}")
         } else {
-            format!("codex resume {escaped}")
+            format!("{command} {escaped}")
         }
     })
 }

+pub fn cli_command_for_action(action: &str) -> String {
+    let cli_name = env::var(CODEX_CLI_NAME_ENV_VAR)
+        .ok()
+        .and_then(|name| {
+            let name = name.trim();
+            if name.is_empty() {
+                None
+            } else {
+                Some(name.to_string())
+            }
+        })
+        .unwrap_or_else(|| "codex".to_string());
+
+    shlex_join(&[cli_name, action.to_string()])
+}
+
 pub fn resume_hint(thread_name: Option<&str>, thread_id: Option<ThreadId>) -> Option<String> {
     let thread_id = thread_id?;
     match thread_name.filter(|name| !name.is_empty()) {
@@ -33,8 +53,39 @@ pub fn resume_hint(thread_name: Option<&str>, thread_id: Option<ThreadId>) -> Op
 mod tests {
     use super::*;
     use pretty_assertions::assert_eq;
+    use serial_test::serial;
+    use std::ffi::OsStr;
+
+    struct EnvVarGuard {
+        key: &'static str,
+        original: Option<std::ffi::OsString>,
+    }
+
+    impl EnvVarGuard {
+        fn set(key: &'static str, value: &OsStr) -> Self {
+            let original = env::var_os(key);
+            unsafe {
+                env::set_var(key, value);
+            }
+            Self { key, original }
+        }
+    }
+
+    impl Drop for EnvVarGuard {
+        fn drop(&mut self) {
+            match self.original.take() {
+                Some(value) => unsafe {
+                    env::set_var(self.key, value);
+                },
+                None => unsafe {
+                    env::remove_var(self.key);
+                },
+            }
+        }
+    }

     #[test]
+    #[serial]
     fn prefers_name_over_id() {
         let thread_id = ThreadId::from_string("123e4567-e89b-12d3-a456-426614174000").unwrap();
         let command = resume_command(Some("my-thread"), Some(thread_id));
@@ -42,6 +93,7 @@ mod tests {
     }

     #[test]
+    #[serial]
     fn formats_thread_id_when_name_is_missing() {
         let thread_id = ThreadId::from_string("123e4567-e89b-12d3-a456-426614174000").unwrap();
         let command = resume_command(/*thread_name*/ None, Some(thread_id));
@@ -52,12 +104,14 @@ mod tests {
     }

     #[test]
+    #[serial]
     fn returns_none_without_a_resume_target() {
         let command = resume_command(/*thread_name*/ None, /*thread_id*/ None);
         assert_eq!(command, None);
     }

     #[test]
+    #[serial]
     fn quotes_thread_names_when_needed() {
         let command = resume_command(Some("-starts-with-dash"), /*thread_id*/ None);
         assert_eq!(
@@ -73,6 +127,7 @@ mod tests {
     }

     #[test]
+    #[serial]
     fn resume_hint_names_picker_item_with_id() {
         let thread_id = ThreadId::from_string("123e4567-e89b-12d3-a456-426614174000").unwrap();
         let hint = resume_hint(Some("my-thread"), Some(thread_id));
@@ -86,6 +141,7 @@ mod tests {
     }

     #[test]
+    #[serial]
     fn resume_hint_uses_direct_id_command_without_name() {
         let thread_id = ThreadId::from_string("123e4567-e89b-12d3-a456-426614174000").unwrap();
         let hint = resume_hint(/*thread_name*/ None, Some(thread_id));
@@ -96,8 +152,34 @@ mod tests {
     }

     #[test]
+    #[serial]
     fn resume_hint_requires_thread_id() {
         let hint = resume_hint(Some("my-thread"), /*thread_id*/ None);
         assert_eq!(hint, None);
     }
+
+    #[test]
+    #[serial]
+    fn resume_command_uses_cli_name_env_override() {
+        let _guard = EnvVarGuard::set(CODEX_CLI_NAME_ENV_VAR, OsStr::new("marathon"));
+        let thread_id = ThreadId::from_string("123e4567-e89b-12d3-a456-426614174000").unwrap();
+        let command = resume_command(/*thread_name*/ None, Some(thread_id));
+        assert_eq!(
+            command,
+            Some("marathon resume 123e4567-e89b-12d3-a456-426614174000".to_string())
+        );
+    }
+
+    #[test]
+    #[serial]
+    fn cli_command_for_action_quotes_cli_name_env_override() {
+        let _guard = EnvVarGuard::set(
+            CODEX_CLI_NAME_ENV_VAR,
+            OsStr::new("/tmp/My Marathon/bin/marathon"),
+        );
+        assert_eq!(
+            cli_command_for_action("resume"),
+            "'/tmp/My Marathon/bin/marathon' resume"
+        );
+    }
 }
