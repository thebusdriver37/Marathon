diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_line_tokens_per_second_footer.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_line_tokens_per_second_footer.snap
index 7ac2c08c29..2ad47cfcb6 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_line_tokens_per_second_footer.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__status_line_tokens_per_second_footer.snap
@@ -7,4 +7,4 @@ expression: normalized_backend_snapshot(terminal.backend())
 "                                                "
 "› Ask Codex to do anything                      "
 "                                                "
-"  21.0 tok/s                                    "
+"  21 tok/s avg · first token 2.5s               "
diff --git a/codex-rs/tui/src/chatwidget/status_controls.rs b/codex-rs/tui/src/chatwidget/status_controls.rs
index f348d6a431..a5d0e1f5d4 100644
--- a/codex-rs/tui/src/chatwidget/status_controls.rs
+++ b/codex-rs/tui/src/chatwidget/status_controls.rs
@@ -412,15 +412,18 @@ impl ChatWidget {
     }
 
     pub(super) fn status_line_tokens_per_second(&self) -> Option<String> {
-        let sample = self.turn_throughput.sample(Instant::now()).map(|sample| {
-            let prefix = if sample.estimated { "~" } else { "" };
-            format!("{prefix}{:.1} tok/s", sample.tokens_per_second)
-        });
-        match (self.turn_throughput.generation_stage(), sample) {
-            (Some(stage), Some(sample)) => Some(format!("{} · {sample}", stage.label())),
-            (Some(stage), None) => Some(stage.label().to_string()),
-            (None, sample) => sample,
+        if let Some(stage) = self.turn_throughput.generation_stage() {
+            return Some(stage.label().to_string());
         }
+
+        self.turn_throughput.sample(Instant::now()).map(|sample| {
+            let prefix = if sample.estimated { "~" } else { "" };
+            let rate = format!("{prefix}{:.0} tok/s avg", sample.tokens_per_second);
+            self.turn_throughput
+                .time_to_first_output()
+                .map(|latency| format!("{rate} · first token {}", format_latency(latency)))
+                .unwrap_or(rate)
+        })
     }
 
     pub(super) fn status_line_limit_display(
@@ -443,3 +446,12 @@ impl ChatWidget {
         }
     }
 }
+
+fn format_latency(duration: Duration) -> String {
+    let seconds = duration.as_secs_f64();
+    if seconds < 10.0 {
+        format!("{seconds:.1}s")
+    } else {
+        format!("{seconds:.0}s")
+    }
+}
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 7ef3481161..6397dde18e 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -2899,7 +2899,8 @@ async fn status_line_tokens_per_second_renders_completed_turn_rate_snapshot() {
     chat.show_welcome_banner = false;
     chat.config.tui_status_line = Some(vec!["tokens-per-second".to_string()]);
     let started_at = Instant::now() - Duration::from_secs(200);
-    chat.turn_throughput.start_turn();
+    chat.turn_throughput
+        .start_turn(started_at - Duration::from_millis(2_500));
     chat.turn_throughput.record_output_delta(
         "streamed response",
         GenerationStage::Answering,
@@ -2920,7 +2921,10 @@ async fn status_line_tokens_per_second_renders_completed_turn_rate_snapshot() {
     );
     chat.refresh_status_line();
 
-    assert_eq!(status_line_text(&chat), Some("21.0 tok/s".to_string()));
+    assert_eq!(
+        status_line_text(&chat),
+        Some("21 tok/s avg · first token 2.5s".to_string())
+    );
 
     let width = 48;
     let height = chat.desired_height(width);
@@ -2945,13 +2949,13 @@ async fn status_line_tokens_per_second_tracks_generation_stage() {
 
     handle_agent_reasoning_delta(&mut chat, "thinking");
     assert!(
-        status_line_text(&chat).is_some_and(|status| status.starts_with("Thinking")),
+        status_line_text(&chat).is_some_and(|status| status == "Thinking"),
         "reasoning delta should identify thinking"
     );
 
     handle_agent_message_delta(&mut chat, "answering");
     assert!(
-        status_line_text(&chat).is_some_and(|status| status.starts_with("Answering")),
+        status_line_text(&chat).is_some_and(|status| status == "Answering"),
         "answer delta should identify answering"
     );
 }
diff --git a/codex-rs/tui/src/chatwidget/turn_runtime.rs b/codex-rs/tui/src/chatwidget/turn_runtime.rs
index e1976c9766..b8073d563f 100644
--- a/codex-rs/tui/src/chatwidget/turn_runtime.rs
+++ b/codex-rs/tui/src/chatwidget/turn_runtime.rs
@@ -71,7 +71,7 @@ impl ChatWidget {
         self.input_queue.user_turn_pending_start = false;
         self.reset_safety_buffering_for_turn_start();
         self.turn_lifecycle.start(Instant::now());
-        self.turn_throughput.start_turn();
+        self.turn_throughput.start_turn(Instant::now());
         self.transcript.reset_turn_flags();
         self.adaptive_chunking.reset();
         if self.plan_stream_controller.take().is_some() {
@@ -107,9 +107,12 @@ impl ChatWidget {
         delta: &str,
         generation_stage: GenerationStage,
     ) {
+        let stage_changed = self.turn_throughput.generation_stage() != Some(generation_stage);
         self.turn_throughput
             .record_output_delta(delta, generation_stage, Instant::now());
-        self.refresh_status_line();
+        if stage_changed {
+            self.refresh_status_line();
+        }
     }
 
     pub(super) fn complete_turn_response(&mut self, output_tokens: Option<i64>) {
diff --git a/codex-rs/tui/src/chatwidget/turn_throughput.rs b/codex-rs/tui/src/chatwidget/turn_throughput.rs
index 4ed00f7b17..8397e6eecd 100644
--- a/codex-rs/tui/src/chatwidget/turn_throughput.rs
+++ b/codex-rs/tui/src/chatwidget/turn_throughput.rs
@@ -38,6 +38,8 @@ struct ActiveResponse {
 
 #[derive(Debug, Default)]
 pub(super) struct TurnThroughput {
+    turn_started_at: Option<Instant>,
+    time_to_first_output: Option<Duration>,
     active_response: Option<ActiveResponse>,
     completed_output_tokens: u64,
     completed_generation_duration: Duration,
@@ -46,8 +48,9 @@ pub(super) struct TurnThroughput {
 }
 
 impl TurnThroughput {
-    pub(super) fn start_turn(&mut self) {
+    pub(super) fn start_turn(&mut self, now: Instant) {
         *self = Self::default();
+        self.turn_started_at = Some(now);
         self.generation_stage = Some(GenerationStage::Prefill);
     }
 
@@ -57,6 +60,11 @@ impl TurnThroughput {
         generation_stage: GenerationStage,
         now: Instant,
     ) {
+        if self.time_to_first_output.is_none()
+            && let Some(turn_started_at) = self.turn_started_at
+        {
+            self.time_to_first_output = Some(now.saturating_duration_since(turn_started_at));
+        }
         self.generation_stage = Some(generation_stage);
         let response = self.active_response.get_or_insert(ActiveResponse {
             started_at: now,
@@ -104,6 +112,10 @@ impl TurnThroughput {
         self.generation_stage
     }
 
+    pub(super) fn time_to_first_output(&self) -> Option<Duration> {
+        self.time_to_first_output
+    }
+
     pub(super) fn set_generation_stage(&mut self, generation_stage: GenerationStage) {
         self.generation_stage = Some(generation_stage);
     }
diff --git a/codex-rs/tui/src/chatwidget/turn_throughput_tests.rs b/codex-rs/tui/src/chatwidget/turn_throughput_tests.rs
index de0a1a3a97..88d3a8f11d 100644
--- a/codex-rs/tui/src/chatwidget/turn_throughput_tests.rs
+++ b/codex-rs/tui/src/chatwidget/turn_throughput_tests.rs
@@ -12,7 +12,7 @@ fn live_sample_is_estimated_from_streamed_bytes() {
     let started_at = Instant::now();
     let mut throughput = TurnThroughput::default();
 
-    throughput.start_turn();
+    throughput.start_turn(started_at);
     throughput.record_output_delta(
         "1234567890123456789012345678901234567890",
         GenerationStage::Thinking,
@@ -34,7 +34,7 @@ fn completed_response_replaces_estimate_with_exact_usage() {
     let completed_at = started_at + Duration::from_secs(2);
     let mut throughput = TurnThroughput::default();
 
-    throughput.start_turn();
+    throughput.start_turn(started_at);
     throughput.record_output_delta("streamed text", GenerationStage::Answering, started_at);
     throughput.complete_response(Some(30), completed_at);
 
@@ -53,7 +53,7 @@ fn fast_completed_response_still_reports_throughput() {
     let completed_at = started_at + Duration::from_millis(100);
     let mut throughput = TurnThroughput::default();
 
-    throughput.start_turn();
+    throughput.start_turn(started_at);
     throughput.record_output_delta("READY", GenerationStage::Answering, started_at);
     throughput.complete_response(Some(4), completed_at);
 
@@ -71,7 +71,7 @@ fn completed_responses_form_a_generation_time_weighted_turn_average() {
     let started_at = Instant::now();
     let mut throughput = TurnThroughput::default();
 
-    throughput.start_turn();
+    throughput.start_turn(started_at);
     throughput.record_output_delta("first", GenerationStage::Thinking, started_at);
     throughput.complete_response(Some(20), started_at + Duration::from_secs(2));
     throughput.record_output_delta(
@@ -97,7 +97,7 @@ fn starting_a_turn_clears_the_previous_sample() {
     throughput.record_output_delta("streamed text", GenerationStage::Answering, started_at);
     throughput.complete_response(Some(30), started_at + Duration::from_secs(2));
 
-    throughput.start_turn();
+    throughput.start_turn(started_at + Duration::from_secs(3));
 
     assert_eq!(throughput.sample(started_at + Duration::from_secs(3)), None);
     assert_eq!(
@@ -111,7 +111,7 @@ fn generation_stage_tracks_prefill_thinking_and_answering() {
     let started_at = Instant::now();
     let mut throughput = TurnThroughput::default();
 
-    throughput.start_turn();
+    throughput.start_turn(started_at);
     assert_eq!(
         throughput.generation_stage(),
         Some(GenerationStage::Prefill)
@@ -132,3 +132,26 @@ fn generation_stage_tracks_prefill_thinking_and_answering() {
     throughput.finish_turn(started_at + Duration::from_secs(1));
     assert_eq!(throughput.generation_stage(), None);
 }
+
+#[test]
+fn records_time_to_first_output_once() {
+    let started_at = Instant::now();
+    let mut throughput = TurnThroughput::default();
+
+    throughput.start_turn(started_at);
+    throughput.record_output_delta(
+        "first",
+        GenerationStage::Thinking,
+        started_at + Duration::from_millis(2_500),
+    );
+    throughput.record_output_delta(
+        "second",
+        GenerationStage::Answering,
+        started_at + Duration::from_secs(4),
+    );
+
+    assert_eq!(
+        throughput.time_to_first_output(),
+        Some(Duration::from_millis(2_500))
+    );
+}
