diff --git a/codex-rs/tui/src/lib.rs b/codex-rs/tui/src/lib.rs
index fd2db509e1..e0e4d4bf10 100644
--- a/codex-rs/tui/src/lib.rs
+++ b/codex-rs/tui/src/lib.rs
@@ -748,7 +748,21 @@ async fn lookup_session_target_with_app_server(
             .thread_read(thread_id, /*include_turns*/ false)
             .await
         {
-            Ok(thread) => Ok(session_target_from_app_server_thread(thread)),
+            Ok(thread)
+                if app_server.uses_remote_workspace()
+                    || thread.model_provider == config.model_provider_id =>
+            {
+                Ok(session_target_from_app_server_thread(thread))
+            }
+            Ok(thread) => {
+                warn!(
+                    session = id_or_name,
+                    expected_provider = %config.model_provider_id,
+                    actual_provider = %thread.model_provider,
+                    "Refusing to open a session from another model provider"
+                );
+                Ok(None)
+            }
             Err(err) => {
                 warn!(
                     session = id_or_name,
diff --git a/codex-rs/utils/cli/src/resume_command.rs b/codex-rs/utils/cli/src/resume_command.rs
index 868a67ed0b..c98497ca77 100644
--- a/codex-rs/utils/cli/src/resume_command.rs
+++ b/codex-rs/utils/cli/src/resume_command.rs
@@ -42,9 +42,12 @@ pub fn cli_command_for_action(action: &str) -> String {
 pub fn resume_hint(thread_name: Option<&str>, thread_id: Option<ThreadId>) -> Option<String> {
     let thread_id = thread_id?;
     match thread_name.filter(|name| !name.is_empty()) {
-        Some(thread_name) => Some(format!(
-            "codex resume, then select {thread_name} ({thread_id})"
-        )),
+        Some(thread_name) => {
+            let command = cli_command_for_action("resume");
+            Some(format!(
+                "{command}, then select {thread_name} ({thread_id})"
+            ))
+        }
         None => resume_command(/*thread_name*/ None, Some(thread_id)),
     }
 }
